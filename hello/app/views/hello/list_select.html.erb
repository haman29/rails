<table border="1">
  <tr>
    <th>署名</th>
    <th>価格</th>
  </tr>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.price %>円</td>
    </tr>
  <% end %>
</table>
